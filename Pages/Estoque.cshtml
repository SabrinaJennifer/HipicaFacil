@page
@model EstoqueModel

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Gerenciamento de Estoque</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <style>
        /* Estilo para a tabela de produtos */
        table {
            font-family: Arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Estilo para os botões de ação */
        .acao-btn {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 5px 10px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 14px;
            margin: 2px 2px;
            cursor: pointer;
            border-radius: 4px;
        }

        .acao-btn-danger {
            background-color: #dc3545;
        }

        /* Estilo para os botões de ordenação */
        .dropdown-menu {
            min-width: 0;
        }

            .dropdown-menu a {
                font-size: 12px;
            }
    </style>
</head>
<body>
    <h1>Gerenciamento de Estoque</h1>
    <br />

    <form method="post">
        <div class="container text-center">
            <div class="row">
                <div class="col-sm-6">
                    <div class="form-floating">
                        <input type="text" class="form-control" id="nome" placeholder=" " required asp-for="Produto.Nome" />
                        <label for="nome">Nome:</label>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="form-floating">
                        <input type="text" id="fabricante" asp-for="Produto.Fabricante" class="form-control" placeholder=" " />
                        <label for="fabricante">Fabricante:</label>
                    </div>
                </div>
            </div>
            <br />
            <div class="row">
                <div class="col-sm-4">
                    <div class="form-floating">
                        <input type="number" id="quantidade" asp-for="Produto.Quantidade" class="form-control" placeholder=" " />
                        <label for="quantidade">Quantidade:</label>
                    </div>
                </div>
                <div class="col-sm-4">
                    <div class="form-floating">
                        <input type="date" id="validade" asp-for="Produto.Validade" class="form-control" placeholder=" " />
                        <label for="validade">Validade:</label>
                    </div>
                </div>
                <div class="col-sm-4">
                    <button type="submit" class="btn btn-primary btn-lg">Adicionar Produto</button>
                </div>
            </div>
        </div>
    </form>
    <br />
    <h2>Lista de Produtos</h2>
    @if (Model.Produtos.Count > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Nome
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButtonNome" data-bs-toggle="dropdown" aria-expanded="false">
                                Ordenar
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButtonNome">
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Nome', 'asc')">A-Z</a></li>
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Nome', 'desc')">Z-A</a></li>
                            </ul>
                        </div>
                    </th>
                    <th>
                        Fabricante
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButtonFabricante" data-bs-toggle="dropdown" aria-expanded="false">
                                Ordenar
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButtonFabricante">
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Fabricante', 'asc')">A-Z</a></li>
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Fabricante', 'desc')">Z-A</a></li>
                            </ul>
                        </div>
                    </th>
                    <th>
                        Quantidade
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButtonQuantidade" data-bs-toggle="dropdown" aria-expanded="false">
                                Ordenar
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButtonQuantidade">
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Quantidade', 'asc')">Crescente</a></li>
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Quantidade', 'desc')">Decrescente</a></li>
                            </ul>
                        </div>
                    </th>
                    <th>
                        Validade
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButtonValidade" data-bs-toggle="dropdown" aria-expanded="false">
                                Ordenar
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButtonValidade">
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Validade', 'asc')">Crescente</a></li>
                                <li><a class="dropdown-item" href="#" onclick="sortTable('Validade', 'desc')">Decrescente</a></li>
                            </ul>
                        </div>
                    </th>
                    <th>Ação</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var produto in Model.Produtos)
                {
                    <tr>
                        <td>@produto.Nome</td>
                        <td>@produto.Fabricante</td>
                        <td>@produto.Quantidade</td>
                        <td>@produto.Validade</td>
                        <td>
                            <button type="button" class="acao-btn" onclick="mostrarFormEditar('@produto.Nome', '@produto.Fabricante', @produto.Quantidade, '@produto.Validade', @produto.Id)">Editar</button>
                            <!-- Botão de Remover -->
                            <form method="post" style="display: inline;">
                                <input type="hidden" name="IdProduto" value="@produto.Id" />
                                <button type="submit" class="acao-btn acao-btn-danger">Remover</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <br />
        <p>Nenhum produto registrado.</p>
    }

    <!-- Formulário de Edição (inicialmente oculto) -->
    <div id="editForm" style="display: none;">
        <h2>Editar Produto</h2>
        <form method="post" asp-page-handler="Editar">
            <div class="container text-center">
                <div class="row">
                    <div class="col-sm-12">
                        <div class="form-floating">
                            <input type="hidden" id="id" name="ProdutoEdit.Id" />
                            <input type="text" class="form-control" id="nomeEdit" name="ProdutoEdit.Nome" placeholder=" " />
                            <label for="nomeEdit">Nome:</label>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-6">
                        <div class="form-floating">
                            <input type="text" id="fabricanteEdit" name="ProdutoEdit.Fabricante" class="form-control" placeholder=" " />
                            <label for="fabricanteEdit">Fabricante:</label>
                        </div>
                    </div>
                    <div class="col-sm-3">
                        <div class="form-floating">
                            <input type="number" id="quantidadeEdit" name="ProdutoEdit.Quantidade" class="form-control" placeholder=" " />
                            <label for="quantidadeEdit">Quantidade:</label>
                        </div>
                    </div>
                    <div class="col-sm-3">
                        <div class="form-floating">
                            <input type="date" id="validadeEdit" name="ProdutoEdit.Validade" class="form-control" placeholder=" " />
                            <label for="validadeEdit">Validade:</label>
                        </div>
                    </div>
                </div>
                <br />
                <div class="row">
                    <div class="col-sm-12">
                        <button type="submit" class="btn btn-primary btn-lg">Salvar</button>
                    </div>
                </div>
            </div>
        </form>
    </div>

    <!-- Script JavaScript para mostrar o formulário de edição e ordenação da tabela -->
    <script>
        function mostrarFormEditar(nome, fabricante, quantidade, validade, id) {
            document.getElementById("nomeEdit").value = nome;
            document.getElementById("fabricanteEdit").value = fabricante;
            document.getElementById("quantidadeEdit").value = quantidade;
            document.getElementById("validadeEdit").value = validade;
            document.getElementById("id").value = id;

            // Exibir o formulário de edição
            document.getElementById("editForm").style.display = "block";
        }

        function sortTable(columnName, order) {
            const table = document.querySelector('table');
            const tbody = table.querySelector('tbody');
            const rows = [...tbody.rows];

            rows.sort((a, b) => {
                const cellA = a.querySelector(`td:nth-child(${getColumnIndex(columnName)})`).textContent.trim();
                const cellB = b.querySelector(`td:nth-child(${getColumnIndex(columnName)})`).textContent.trim();

                if (!isNaN(cellA) && !isNaN(cellB)) {
                    return order === 'asc' ? cellA - cellB : cellB - cellA;
                } else {
                    return order === 'asc' ? cellA.localeCompare(cellB) : cellB.localeCompare(cellA);
                }
            });

            while (tbody.firstChild) {
                tbody.removeChild(tbody.firstChild);
            }

            rows.forEach(row => tbody.appendChild(row));
        }

        function getColumnIndex(columnName) {
            const headers = document.querySelectorAll('th');
            let index = 0;

            for (const th of headers) {
                index++;
                if (th.textContent.trim() === columnName) {
                    break;
                }
            }

            return index;
        }

    </script>
</body>
</html>

